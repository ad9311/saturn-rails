= form_for recovery, url: "/recoveries/#{recovery.id}/report", method: :patch do |f|
  = f.submit "Submit report", class: "px-3 py-2 text-white rounded-md bg-blue-400 hover:bg-blue-500 cursor-pointer"
